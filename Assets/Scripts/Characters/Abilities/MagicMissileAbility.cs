using UnityEngine;
using System.Collections;

namespace DiabloKiller {
	public class MagicMissile : RangedAbility {

        public MagicMissile(Character owningCharacter, string name) : base(owningCharacter, name) {
        }

		// Use this for initialization
		void Start () {
		
		}
		
		// Update is called once per frame
		void Update () {
		
		}
	}
}